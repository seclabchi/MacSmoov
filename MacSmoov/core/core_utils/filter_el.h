#ifndef __FILTER_EL_H__#define __FILTER_EL_H__#include "biquad.h"/* Equal-loudness bandpass filter (approximating the equal-loudness Fletcher-Munson curves */class FilterEL{  public:    FilterEL(const tonekids::dsp::SOS& _sos1, const tonekids::dsp::SOS& _sos2, const tonekids::dsp::SOS& _sos3, uint32_t _n_samp);    virtual ~FilterEL();    virtual void process(float* in, float* out);  private:    FilterEL();    tonekids::dsp::SOS sos1, sos2, sos3;    tonekids::dsp::Biquad* bq1;    tonekids::dsp::Biquad* bq2;    tonekids::dsp::Biquad* bq3;    float* tmpbuf1, *tmpbuf2;    uint32_t m_nsamp;};#endif  //#ifndef __FILTER_EL_H__