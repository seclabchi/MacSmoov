#pragma once#include <vector>#include <cstdint>#include "butterworth.h"namespace tonekids {namespace dsp {class FilterLR4 {    public:        FilterLR4(double _fs, double _fc, tonekids::dsp::BUTTERWORTH_TYPE _type, uint32_t _n_samp);        bool retune(double _fc, tonekids::dsp::BUTTERWORTH_TYPE _type);        void process(float* in, float* out);        ~FilterLR4();    private:        double fs, fc;        uint32_t n_samp;        float* tmp_buf;        tonekids::dsp::BUTTERWORTH_TYPE type;        FilterLR4();        tonekids::dsp::Butterworth* bw;        std::vector<Biquad> bqv1;        std::vector<Biquad> bqv2;        double bqv1_gain, bqv2_gain;};} /* dsp */} /* tonekids */